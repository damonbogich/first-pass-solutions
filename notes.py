"""
input: int
output: bool

Take in a positive number and decide if the number divides itself
"""


def divides_self(num):
    #if num <= 0:
        #return False
    #elif num < 10:
        # return True
    
    #split num into individual digits
    #loop through each digit in num
        #if digit does not evenly divides into num or if digit = 0 
            #return False
        #if we get through all of the digits return true